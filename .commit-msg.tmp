feat(workbench): 资产域①层 —— 一份集中定义 + 资产根约定（b05 Task 8）

形状与约定落在 `.comate/specs/b05-content-pipeline-charter/asset-domain-design.md`，
数据层与判据落在代码里。

定义（`presets/assets.toml`，现在是空骨架，形状与约定写在头部注释里）：
- 六个字段：`id` / `type` / `machineId?` / `name` / `path`（+ `slicer`·`profile`
  只给切片器预设）
- **刻意不写** `fileName`（旧仓同时存 `fileName` 与 `relativePath`，改名时必有一处忘掉）
  与 `sha256`/`size`（交付时按真实字节算，写进①层就是第二份真相且一定先过期）
- 类型：`image` / `icon` / `model` / `slicerProfile`。**没有 `mkpPreset`** ——
  不是一句注释，是 enum 里没有这一档（doc §12.5），想登记得先改 enum

目录约定：
- 资产根 `<repo>/public/assets/`（vite 静态目录下的唯一子根，前端按 `/assets/<path>` 取）
- `path` **相对资产根**，不相对定义文件：定义在 `presets/`（①层）、载荷在
  `public/assets/`（②层），路径基准只该有一处
- 根按需建：`resolve_in` 的第三道要比真实路径，根不存在则每条 path 都解析失败
  （空目录靠 `public/assets/.gitkeep` 进库）

判据（每条都带反空转输入）：
- id 唯一且**大小写不敏感**（`a1-image` vs `A1-Image` 实测红）
- `path` 落在资产根内（`../` / 绝对路径 / `a/../../b` 三条实测红）
- 键名写错要响亮（`deny_unknown_fields`：`pth` 会变成"没有 path"，不是静默少一项）
- 切片器字段搭配；归属机型必须是真机型（跨文件那条进 `check_cross_consistency`）
- 零编辑往返逐字节相同；`add` 与加载期**走同一套检查**（越界路径在加法上也要被拦）
- **①层 `.toml` 必须是 LF**：工具新建文件会写成 CRLF，而 `toml_edit` 写回统一成 LF ⇒
  第一次保存整份被改写、diff 一片红。本轮 `presets/assets.toml` 正是被这条抓出来的
  （`.gitattributes` 管得住入库那一份，管不住工作区那一份）

端到端探针（实测）：往真 `presets/assets.toml` 塞一条 `machineId = 'NOPE'` 的条目 →
所有加载类判据一起红（`Presets::load_from` 走 `check_cross_consistency`）；探针已撤，
文件与哈希复原。

接入：
- `Presets.assets` + `check_cross_consistency`；`wb_assets`（**只读**）+ DTO
- 前端 `AssetKind` / `AssetView` / `AssetList` + `wb.assets()` + `assetUrl()`
  （BBS 路径里有空格，编码只做一次）

**8.6（机型 `image`/`icon` 改指资产 id）挪到 Task 9**（裁决）：改引用前必须已有 id 与
文件，否则造出一批"指向不存在资产的引用" —— 而那正是 9.7 要拦的东西。Task 9 一次做完
三件事：搬文件 → 写条目 → 改引用。

复验：`cargo test -p mkp-support-ease --features workbench` 277/277（+11）；
工作区默认 feature 全绿；`cargo fmt --all -- --check` 与两档
`cargo clippy --all-targets -- -D warnings` 全过。

未推送。
