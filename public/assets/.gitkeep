# 资产根（b05 Task 8 定的约定，见 presets/assets.toml 的头部注释）。
#
# 我们管的资产文件全在这个目录下：机型图、图标、模型、BBS 预设 JSON。
# 条目里的 `path` 永远相对这里，前端按 `/assets/<path>` 取。
#
# 现在是空的：文件与条目一起在 b05 Task 9 落地。
# 这个占位文件留着是为了让**空目录也能进库** —— `resolve_in` 的第三道要
# 比真实路径，根不存在的话每条 `path` 都会解析失败。
