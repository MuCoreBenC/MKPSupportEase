# 换行符：**这份文件是给 Windows 那台机器准备的保险**。
#
# Git for Windows 默认 `core.autocrlf=true` —— clone 时把文本文件的 LF 换成 CRLF。
# `scripts/hooks/*` 是 `#!/bin/sh` 脚本，一旦变成 CRLF，shebang 就成了 `/bin/sh\r`，
# 内核找不到这个解释器，钩子**静默不执行**（git 只打一行 hint）。
# 那正是试验场那七道闸空转的同一类失效，只是换了个成因。
#
# 所以这里把规则写死在仓库里，不依赖每台机器的 git 配置。

# 默认：文本由 git 判断，工作区一律 LF
* text=auto eol=lf

# 这几类必须是 LF，没有例外
*.sh text eol=lf
scripts/hooks/* text eol=lf
*.mjs text eol=lf
*.js text eol=lf
*.ts text eol=lf
*.tsx text eol=lf
*.rs text eol=lf
*.toml text eol=lf
*.json text eol=lf
*.css text eol=lf
*.md text eol=lf
*.yml text eol=lf

# Windows 专属脚本反过来：必须 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# 二进制：别碰
*.png binary
*.webp binary
*.svg text eol=lf
*.glb binary
*.icns binary
*.ico binary
*.dmg binary
*.pdf binary
