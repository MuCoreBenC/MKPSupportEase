{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "核心权限 + 拖动标题栏。前端不碰文件系统 —— 读写都经过 src-tauri 的命令，fsx::paths::resolve 把路径夹在两个数据根内，所以不需要 fs:default 也不需要逐条 fs 权限。core:window:allow-start-dragging 是 data-tauri-drag-region 能工作的前提：core:default 里不含它，缺了的表现就是整条标题栏拖不动、而且没有任何报错。",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
